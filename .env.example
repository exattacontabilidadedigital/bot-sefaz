# =============================================================================
# CONFIGURAÇÕES DO BOT SEFAZ - DEPLOYMENT
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para o bot.
# Copie este arquivo para .env e preencha com suas credenciais reais.
# NUNCA commite o arquivo .env com senhas reais!

# -----------------------------------------------------------------------------
# CREDENCIAIS DE ACESSO SEFAZ
# -----------------------------------------------------------------------------
# Usuário e senha para login no sistema SEFAZ
# IMPORTANTE: Para Gmail/Outlook, use App Password ao invés da senha normal
# Gmail: https://myaccount.google.com/apppasswords
# Outlook: https://account.live.com/proofs/AppPassword
USUARIO=seu_usuario_aqui
SENHA=sua_senha_aqui

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO BANCO DE DADOS
# -----------------------------------------------------------------------------
# Caminho do arquivo SQLite (use caminho absoluto em produção)
# Docker/Coolify: /app/data/sefaz_consulta.db
# Local: sefaz_consulta.db
DB_PATH=sefaz_consulta.db

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO NAVEGADOR
# -----------------------------------------------------------------------------
# HEADLESS: Executar navegador sem interface gráfica
# - false: Modo visual (desenvolvimento/debug)
# - true: Modo headless (produção/servidor)
HEADLESS=false

# TIMEOUT: Tempo máximo de espera por elementos (em milissegundos)
TIMEOUT=30000

# -----------------------------------------------------------------------------
# URL DO SISTEMA SEFAZ
# -----------------------------------------------------------------------------
SEFAZ_URL=https://sefaznet.sefaz.ma.gov.br/sefaznet/login.do?method=prepareLogin

# -----------------------------------------------------------------------------
# SMTP (OPCIONAL) - NOTIFICAÇÕES POR E-MAIL
# -----------------------------------------------------------------------------
# Configurações para envio de notificações por e-mail
# Deixe vazio para desabilitar notificações

# Gmail:
# - SMTP_HOST=smtp.gmail.com
# - SMTP_PORT=587
# - SMTP_USER=seuemail@gmail.com
# - SMTP_PASS=sua_app_password_aqui

# Outlook:
# - SMTP_HOST=smtp-mail.outlook.com
# - SMTP_PORT=587
# - SMTP_USER=seuemail@outlook.com
# - SMTP_PASS=sua_senha_aqui

SMTP_HOST=smtp.exemplo.com
SMTP_PORT=587
SMTP_USER=seu_usuario_smtp
SMTP_PASS=sua_senha_smtp
SMTP_FROM=no-reply@seu-dominio.com
SMTP_TLS=true
NOTIFY_TO=fiscal@exattacontabilidade.com.br

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE DEPLOY (DOCKER/COOLIFY)
# -----------------------------------------------------------------------------
# Porta do servidor (padrão: 8000)
PORT=8000

# Host do servidor (padrão: 0.0.0.0 para aceitar todas conexões)
HOST=0.0.0.0

# Ambiente (development, production)
ENVIRONMENT=production

# Número de workers (deixe em branco para auto-detect)
WORKERS=1