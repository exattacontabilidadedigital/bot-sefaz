# Configuração para produção (servidor hospedado)
# Copie este arquivo para .env e ajuste os valores

# Caminho do banco de dados
# Se estiver usando volume nomeado /sefaz_sqlite_data, configure:
DB_PATH=/sefaz_sqlite_data/sefaz_consulta.db

# Ou se estiver usando o volume padrão do docker-compose:
# DB_PATH=/app/sefaz_consulta.db

# Ambiente
ENVIRONMENT=production

# Configuração do Bot
HEADLESS=true
SEFAZ_URL=https://sefaznet.sefaz.ma.gov.br/sefaznet/login.do
TIMEOUT=30000

# Email (Opcional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TLS=true
NOTIFY_TO=
